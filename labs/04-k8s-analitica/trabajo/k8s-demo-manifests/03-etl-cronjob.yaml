apiVersion: batch/v1
kind: CronJob
metadata:
  name: etl-cron
  namespace: analytics-data
spec:
  schedule: "*/1 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: etl
              image: ghcr.io/TU_USUARIO/docker-k8s-101/etl:v1.0.0
              env:
                - name: DATABASE_URL
                  value: postgresql://analytics:analytics@db:5432/analytics